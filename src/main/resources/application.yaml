server:
  port: 8081
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/myrealm
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/e_commerce
    username: postgres
    password: Piseth1711*#
  application:
    name: advanced-api
file-upload:
  server-path: D:/ISTAD/Year 2/Semester 2/Back-end Dev II/Code/fileupload/
